<script>
  Push.create(
    'チケットが発券されています', {
      body: "こちらの利用画面を店員さんに見せてください",
      icon: '', // 画像
      timeout: 12 * 3600, // 通知が消えるタイミング
      vibrate: [100, 100, 100], // モバイル端末でのバイブレーション秒数
      onClick: function() {
        location.href = "/users/<%= current_user.id %>/tickets/<%= current_user.ticket.id %>";
        console.log(this);
      }
    }
  );
</script>
